<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examen2EvHugo"
        tools:targetApi="31">
        <activity
            android:name=".views.ChisteSeleccionadoActivity"
            android:exported="false"
            android:label="@string/title_activity_chiste_seleccionado"
            android:theme="@style/Theme.Examen2EvHugo.NoActionBar" />
        <activity
            android:name=".views.ChistesDetail"
            android:exported="false"
            android:label="@string/title_activity_chistes_detail"
            android:theme="@style/Theme.Examen2EvHugo.NoActionBar" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Examen2EvHugo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>